function [ NashStable ] = isNashStable( )

partition=[[1,2] 1, [1,2,3]];
part=zeros(length(partition));

for i=1:length(partition)
    part(i)=
end

tmp=num2cell(partition);
[a b c d e f]=deal(tmp{:});
disp('tmp');
disp(tmp);
NashStable=false;
disp('cellArr');
disp(cellArr);

for i=1:length(partition)-1
    for j=1+1:length(partition)
        %if(utility())
        end
    end
end


