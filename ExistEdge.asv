function [ output_args ] = ExistEdge( graph, service1, service2 )

A=full(graph);
Are

for i=1:size(A,1)
  for j=1:size(A,2)
    if(A(service1,service2)~=0)
    utility=utility+A(i,j);
    end
  end
end

end

