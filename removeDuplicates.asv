function [ output_args ] = removeDuplicates( partition_with_repetitions )

for i=0:partition_with_repetitions.size()-1
    for j=1:partition_with_repetitions.size()-2
        if(partition_with_repetitions(i)==partition_with_repetitions(j))
            partition_with_repetitions.remove();
        end
    end
end

end

